import 'package:flutter/material.dart';

const textColor = Colors.white;
const textDarkBlue = Color(0xff003D63);
const greyText = Color(0xff8D989F);
const blurBlue = Color(0xa40c72b0);
const iconTile = Color(0xccD9D9D9);
const backgroundContainer=Color.fromRGBO(62, 57, 88, 10);

const backgroundScreen = Color.fromRGBO(10, 31, 60, 1);

LinearGradient gradientColor = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.topRight,
  colors: [
    Color.fromRGBO(0, 0, 25, 1),
    Color.fromRGBO(10, 31, 60, 1),
  ],
);

